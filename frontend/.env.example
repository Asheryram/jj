# Copy to .env.local to run against the local API.
#
# Set    → every read and write goes to the NestJS API and Postgres.
# Unset  → the app runs on the in-memory mock in src/data/mock.ts, with no
#          backend needed. Useful for reviewing UI changes on their own.
VITE_API_URL=http://localhost:3001/api

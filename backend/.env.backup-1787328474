# JamesDataConsult — backend configuration
#
# Copied from .env.example. Values here are real; do not commit this file.

# ── Database ────────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://jdc:jdc_dev_password@localhost:5433/jdc?schema=public"

# ── Server ──────────────────────────────────────────────────────────────────
PORT=3001

# Origins allowed to call this API. The front end, and nothing else.
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"

# Where the browser reaches the front end. Used for Paystack's return URL and
# for the one-time password links, so a wrong value sends people nowhere.
# Set this to your ngrok URL while testing, and your domain in production.
PUBLIC_APP_URL="https://d6f7-196-61-44-164.ngrok-free.app"
# ── Sessions ────────────────────────────────────────────────────────────────
JWT_SECRET="dev-only-secret-do-not-use-in-production"
JWT_EXPIRES_IN="12h"

# ── Platform access ─────────────────────────────────────────────────────────
# Who runs the platform. On boot this address is made a superadmin and emailed a
# one-time link to set their own password. They then create the admin the same
# way. No password is ever seeded.
SUPERADMIN_EMAIL="ashertettehabotsi@gmail.com"

# ── Email (Resend) ──────────────────────────────────────────────────────────
RESEND_API_KEY="re_D8FmygN7_5ctqQky4PyNokTP5QP1a69hg"

# ── Email: SMTP (Gmail) ─────────────────────────────────────────────────────
# Used in preference to Resend when set, because it can reach anybody — Resend
# refuses every recipient but your own account until a domain is verified.
#
# For Gmail you need an App Password, not your normal password:
#   1. Turn on 2-Step Verification at myaccount.google.com/security
#   2. Go to myaccount.google.com/apppasswords
#   3. Create one named e.g. "JamesDataConsult" and paste the 16 characters below
#      (spaces are fine, they are stripped)
#
# A free Gmail account sends roughly 500 messages a day. Fine for admin, agent
# and reset links; not for customer notifications at volume.
SMTP_USER="vshxrdev@gmail.com"
SMTP_PASS="hrbb timy rrht mqpw"

# Defaults suit Gmail. Change only for another provider.
#SMTP_HOST=smtp.gmail.com
#SMTP_PORT=465

# Display name only, when SMTP is in use — the address is forced to SMTP_USER,
# because Gmail rewrites or refuses anything else.
# Must be an address on a domain you have verified at resend.com/domains.
# Left empty it falls back to Resend's shared sender, which reaches ONLY the
# address that owns your Resend account — enough for your own setup links, not
# for agents or customers. A free-mail address (gmail.com and the like) can
# never be used here: you cannot verify a domain you do not control.
MAIL_FROM=""

# ── Supplier: DataHub GH ────────────────────────────────────────────────────
# false keeps fulfilment simulated in-process. true spends real money.
DATAHUB_LIVE=true
DATAHUB_API_KEY="sk_9ff6a12f22b4e4cc0f4b4abf33ea23f1b1b1f4865b22cab6"
DATAHUB_BASE_URL="https://app.datahubgh.com/api"

# Register with DataHub as <api origin>/api/webhooks/datahub/<this secret>.
# Their callback is unauthenticated, so the secret in the path is what proves it.
DATAHUB_WEBHOOK_SECRET="vlAr7dIFf9JsT5wvdINr72RNN1nnH89uKfwMuPyDgv0"

# ── Payments: Paystack ──────────────────────────────────────────────────────
# Webhook: <api origin>/api/payments/paystack/webhook — HMAC-verified, no secret
# of its own needed.
PAYSTACK_SECRET_KEY="sk_live_71eb3ec8c98ea57b2ba15fcd238cfcb8727ef8e0"

# ── Optional ────────────────────────────────────────────────────────────────
# How long a paid order waits for the recipient's number to be approved before
# it is refunded. Defaults to 6. Set 0 to refund immediately.
#APPROVAL_HOLD_HOURS=6

# How long simulated fulfilment takes, in ms. Only used when DATAHUB_LIVE=false.
FULFILMENT_DELAY_MS=2600
